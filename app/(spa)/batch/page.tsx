import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Batch crop · WMC',
  description:
    'Drop multiple files, pick presets, download a zip with every (file × preset) crop. Subject-aware, in your browser.',
  robots: { index: false, follow: false },
}

export default function Page() {
  return null
}
