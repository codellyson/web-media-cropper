import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Studio · WMC',
  description:
    'Subject-aware multi-platform crop. Drop a file to start. In your browser, no upload.',
  robots: { index: false, follow: false },
}

export default function Page() {
  return null
}
