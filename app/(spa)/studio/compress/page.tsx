import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Compress · WMC',
  description: 'Compress images to a target size, in your browser.',
  robots: { index: false, follow: false },
}

export default function Page() {
  return null
}
